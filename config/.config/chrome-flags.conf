--ozone-platform=wayland
--enable-wayland-ime

# Keep GPU video decode + compositing, raster on CPU: chrome's GPU shader path
# faulted the Radeon 680M (amdgpu page fault -> GPU reset, display stuck).
# Jun 2 + Jun 6 2026. Escalate to --disable-gpu if it recurs.
--disable-gpu-rasterization
